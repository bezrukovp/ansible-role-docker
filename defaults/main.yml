---
docker_with_linux_image: no
docker_opts: "--selinux-enabled -H tcp://0.0.0.0:2376 -H unix:///var/run/docker.sock"
docker_bridge_ip:
docker_mirror_host:
docker_mirror_protocol: "https"
docker_mirror_port: 5000
docker_compose_version: 1.10.0
